Add progress phases between send and first token

Emits machine-named phases (preparing, reading-page, sending, thinking,
responding) over the agent port; the panel maps them to localized status
text and replaces a single transient line, clearing it when the first
delta/tool result arrives. This makes the gap after pressing Send visible
instead of looking like a hang. Also routes the unattended agent phases
to its step callback.
